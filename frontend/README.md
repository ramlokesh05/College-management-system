# Frontend - Rusheel College Management System

## Setup
1. Create `frontend/.env` from `frontend/.env.example`.
2. Install packages:
   - `npm install`
3. Start dev server:
   - `npm run dev`

## Build and Lint
- `npm run lint`
- `npm run build`

